define(["jquery","math"], function($, _math) {
    //the jquery.alpha.js and jquery.beta.js plugins have been loaded.
    $(function() {
        $().jquery;

        var result = _math.add( 100, 90);
        $("#name").val(result);
    });
});