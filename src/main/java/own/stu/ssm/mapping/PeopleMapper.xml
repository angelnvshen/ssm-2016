<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="own.stu.ssm.dao.IPeopleDao" >

    <resultMap id="BaseResultMap" type="own.stu.ssm.model.People" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="user_name" property="userName" jdbcType="VARCHAR" />
        <result column="pass_word" property="passWord" jdbcType="VARCHAR" />
        <result column="age" property="age" jdbcType="INTEGER" />
    </resultMap>

    <sql id="Base_Column_List" >
      id, user_name, pass_word, age
    </sql>

    <select id="selectByCountryQueryModel" parameterType="String" resultMap="BaseResultMap">
        select
         <include refid="Base_Column_List" />
        from t_people
        <where>
            <if test="userName != null and userName != ''">
                user_name like concat('%',concat(#{userName}, '%'))
            </if>
            <if test="passWord != null">
                and pass_word = #{passWord}
            </if>
            <if test="id != null">
                and id = #{id}
            </if>
        </where>
    </select>
</mapper>